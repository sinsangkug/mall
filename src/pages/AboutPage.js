// AboutPage 컴포넌트를 정의합니다. 이 컴포넌트는 "About Page"라는 텍스트를 큰 글씨 크기(3xl)로 화면에 표시합니다.
const AboutPage = () => {
    return <div className="text-3xl">About Page</div>
}

// AboutPage 컴포넌트를 다른 파일에서 사용할 수 있도록 export 합니다.
export default AboutPage;
